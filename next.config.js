// Importing the type definitions for Next.js config
/** @type {import('next').NextConfig} */

// Exporting the configuration for the Next.js application
module.exports = {
  // Enabling React's strict mode, which is a way to opt in to a more restrictive variant of React to detect potential problems
  reactStrictMode: true,
}
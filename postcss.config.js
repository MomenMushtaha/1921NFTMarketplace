// Exporting the configuration for PostCSS
module.exports = {
  // Defining the plugins to be used by PostCSS
  plugins: {
    // Including the Tailwind CSS plugin
    tailwindcss: {},

    // Including the Autoprefixer plugin, which automatically adds vendor prefixes to CSS rules
    autoprefixer: {},
  },
}
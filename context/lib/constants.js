// Importing necessary libraries and components
import abi from './Transactions.json'

// The ABI (Application Binary Interface) is essentially a JSON representation 
// of the smart contract, including all of its functions and variables.
export const contractABI = abi.abi

// The contract address is the address at which the smart contract is deployed on the blockchain.
export const contractAddress = '0x195fcD4a17434da34a374b373D943c071b783CaF'